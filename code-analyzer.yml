engines:
  pmd:
    disable_engine: true
  cpd:
    disable_engine: true
  sfge:
    disable_engine: true
  flow:
    disable_engine: true

rule-selectors:
  - eslint:Recommended

workspace:
  - .

targets:
  - force-app/main/default/lwc/gptProfileComponent
